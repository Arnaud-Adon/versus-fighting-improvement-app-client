import React from "react";
import { View, Text } from "react-native";

const GoogleAuthForm = (props) => {
  return (
    <View>
      <Text>Google Authentification</Text>
    </View>
  );
};

export default GoogleAuthForm;
