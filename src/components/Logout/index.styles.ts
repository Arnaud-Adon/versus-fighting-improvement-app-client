import { StyleSheet } from 'react-native';

export const { container, image } = StyleSheet.create({
  container: {
    paddingHorizontal: 10,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'flex-end',
  },
  image: {
    width: 30,
    height: 30,
  },
});
